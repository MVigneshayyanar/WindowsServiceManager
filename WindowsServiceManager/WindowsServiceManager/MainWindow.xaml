<Window
    x:Class="WindowsServiceManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsServiceManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="20">
        <TextBlock Text="Windows Service Manager" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        <Button Content="Refresh Services" Click="RefreshServices_Click" Margin="0,0,0,10"/>
        <ListView x:Name="ServiceListView" Height="600">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ServiceModel">
                    <StackPanel Padding="5" BorderBrush="Gray" BorderThickness="0 0 0 1">
                        <TextBlock Text="{x:Bind ServiceName}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind Status}"/>
                        <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>
