<Window
    x:Class="WindowsServiceManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsServiceManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Windows Service Manager">

    <StackPanel Padding="20" Spacing="10">
        <TextBlock Text="Windows Service Manager" FontSize="24" FontWeight="Bold"/>
        <TextBox x:Name="SearchBox" PlaceholderText="Search by service name..." TextChanged="SearchBox_TextChanged"/>
        <Button Content="Refresh Services" Click="RefreshServices_Click"/>

        <ListView x:Name="ServiceListView" Height="500" SelectionMode="Single">
            <ListView.Header>
                <Grid ColumnDefinitions="1*,1*,2*,1*,1*">
                    <TextBlock Text="Service Name" Grid.Column="0"/>
                    <TextBlock Text="Status" Grid.Column="1"/>
                    <TextBlock Text="Description" Grid.Column="2"/>
                    <TextBlock Text="Startup Type" Grid.Column="3"/>
                    <TextBlock Text="Log On As" Grid.Column="4"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ServiceModel">
                    <Grid ColumnDefinitions="1*,1*,2*,1*,1*">
                        <TextBlock Text="{x:Bind ServiceName}" Grid.Column="0"/>
                        <TextBlock Text="{x:Bind Status}" Grid.Column="1"/>
                        <TextBlock Text="{x:Bind Description}" Grid.Column="2"/>
                        <TextBlock Text="{x:Bind StartupType}" Grid.Column="3"/>
                        <TextBlock Text="{x:Bind LogOnAs}" Grid.Column="4"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
            <Button Content="Start" Click="StartService_Click"/>
            <Button Content="Stop" Click="StopService_Click"/>
            <Button Content="Pause" Click="PauseService_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
